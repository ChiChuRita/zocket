{
  "name": "zocket",
  "type": "module",
  "scripts": {
    "build": "bun run --workspaces build",
    "clean": "bunx tsdown -W --clean"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "tsdown": "^0.16.1",
    "typescript": "^5.0.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
